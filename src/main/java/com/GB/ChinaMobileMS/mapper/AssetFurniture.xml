<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.GB.ChinaMobileMS.dao.AssetFurnitureMapper">
 
 <insert id="addFurniture" parameterType="AssetFurniture">
		INSERT INTO
		office_furniture_info
		(house_id,furniture_type,brand,specification,used_branch_id,count,used_situation)
		VALUES
		(#{houseId},#{furnitureType},#{brand},#{specification},#{usedBranchId},#{count},#{usedSituation})
	</insert>
 <select id="queryAssetFurniture" resultType="AssetFurniture">
		SELECT
		o.used_branch_id,
		b.branch_name,	
		o.house_id,
		o.furniture_type,
		o.brand,
		o.specification,
		o.count,
		o.office_furniture_info_id,
		h.floor
		FROM branch b,
		house_info h,
		office_furniture_info o
		WHERE b.branch_id =
		o.used_branch_id
		And h.house_id = 
		o.house_id
	</select>
 </mapper>
 